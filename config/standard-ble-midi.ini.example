# Deprecated legacy single-file example.
#
# Current runtime validation uses the directory layout:
#
#   daemon.ini
#   devices.d/*.ini
#
# Use:
#
#   config/daemon.ini.example
#   config/devices.d/standard-ble-midi.ini.example
#
# This file remains only as a compatibility marker for older documentation and
# should not be used with the current config-directory runtime.
